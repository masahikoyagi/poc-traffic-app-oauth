# Uncomment the next line to define a global platform for your project
  platform :ios, '10.0'

target 'poc-traffic-app-oauth' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'OAuthSwift', '~> 1.2.0'

  # Pods for poc-traffic-app-oauth

  target 'poc-traffic-app-oauthTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'poc-traffic-app-oauthUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
